<domains>: include
#<domains/apronWrapper.ml*>: use_apron
<*.ml*>: package(zarith)
<domains/*.ml*>: package(zarith)
<domains/old/*.ml*>: package(zarith)
<main.{byte,native}>: use_nums, use_unix
#<main.{byte,native}>: use_gmp, use_apron, use_polka, use_oct, use_box, 
<main.{byte,native}>: package(zarith)
#<main.{byte,native}>: package(osdp), package(glpk), package(ocamlgraph)
